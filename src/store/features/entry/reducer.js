const initState = {
  counter: 0
}

function reducer(state = initState, action) {
  switch (action.type) {
    default:
      return state
  }
}

export { reducer }
export default reducer
